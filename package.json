{
  "scripts": {
    "server": "json-server --watch json-server/db.json --port 8086 --host localhost --routes json-server/routes.json"
  },
  "dependencies": {},
  "devDependencies": {
    "json-server": "^0.17.3"
  }
}
